# ============================================================
# Express 后端构建上下文排除清单
# ============================================================
# 本目录的 Dockerfile 是 `COPY . .`，所以这份清单决定「哪些文件会进镜像」。
# ⚠️ 别写 `\*.log` 这种转义写法 —— 实测（最小构建对照）它**不会**排除 .log 文件，
#    因为 dockerignore 用 Go 的 filepath.Match 语义，`\` 是转义符而不是通配符前缀。
# ============================================================

node_modules/
.git/

# 本地环境文件：.env 是真实密钥，.env.example 是模板 —— 镜像里两个都用不到，
# 配置由 docker-compose 的 environment 直接注入容器
.env*

logs/
test/
.vscode/
.idea/

*.log
