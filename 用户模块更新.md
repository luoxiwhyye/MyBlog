# MyBlog 项目更新规划：访客评论模块增强

## 一、更新目标

为个人博客系统增加完善的访客评论体验，支持访客通过**昵称 + 邮箱 + 网址**的方式发表评论，并通过邮箱自动获取 Gravatar 头像。

**核心原则**：

- 访客无需注册/登录
- 不引入访客用户表
- 仅博主可管理/删除评论（暂不实现访客自删）
- 最大程度复用现有数据库和代码结构

---

## 二、数据库变更

### 需要修改的表：`comment`

| 操作 | 字段名       | 类型                | 说明                                                    |
| ---- | ------------ | ------------------- | ------------------------------------------------------- |
| 新增 | `author_url` | `varchar(500) NULL` | 访客个人博客/网站地址（选填），位于 `author_email` 之后 |

### 变更说明

- 现有 `author_name` 和 `author_email` 保持原样
- `author_ip` 继续由后端自动记录
- 无需新增任何索引或外键
- 无需创建新表

### 可选变更（暂不实施）

- `delete_token` 字段（用于访客自删评论，本次不做）
- `visitor` 访客信息表（本次不做）

---

## 三、后端 API 变更（myblog-express）

### 需要修改的接口：`POST /api/v1/comments`

#### 请求参数变更

**新增可选字段**：
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `author_url` | string | 否 | 访客个人网站地址，需做 URL 格式校验 |

**现有字段保持不变**：
| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| `article_id` | int | 是 | 文章ID |
| `parent_id` | int | 否 | 父评论ID |
| `author_name` | string | 是 | 昵称（2-50字符） |
| `author_email` | string | 是 | 邮箱（格式校验） |
| `content` | string | 是 | 评论内容（1-1000字符） |

#### 业务逻辑变更

1. **参数校验**：增加 `author_url` 的可选 URL 格式校验
2. **数据存储**：将 `author_url` 写入 `comment` 表
3. **IP 获取**：保持不变，从请求头获取
4. **状态默认值**：`pending`（待审核），与现有逻辑一致
5. **响应**：评论提交成功后，返回完整的评论对象（包含 `author_url`）

#### 不需要修改的地方

- 评论列表查询接口（`GET /comments`）会自动返回新字段
- 评论状态管理接口（`PUT /comments/:id/status`）无需变更
- 点赞接口（`POST /comments/:id/like`）无需变更

---

## 四、前端博客变更（myblog-blog）

### 4.1 评论表单组件

**位置**：`components/` 目录下的评论表单组件（如 `CommentForm.vue`）

#### UI 变更

在现有表单基础上增加“网址”输入框：

| 现有字段         | 新增字段     |
| ---------------- | ------------ |
| 昵称（必填）     | 网址（选填） |
| 邮箱（必填）     | -            |
| 评论内容（必填） | -            |

#### 布局建议

- 昵称和邮箱并排（两列布局）
- 网址独占一行（放在邮箱下方）
- 保持与现有风格一致的间距和圆角

#### 交互行为

- 网址输入框显示占位符：`https://yourblog.com`
- 提交前做简单的 URL 格式校验（前端友好提示，不阻塞提交）
- 可考虑将访客最后一次填写的信息存储到 localStorage，下次自动填充（用户体验优化）

### 4.2 评论列表组件

**位置**：`components/` 目录下的评论列表组件（如 `CommentList.vue`）

#### 展示变更

每条评论需要展示：

| 元素     | 数据来源                  | 说明                           |
| -------- | ------------------------- | ------------------------------ |
| 头像     | `author_email` → Gravatar | 使用邮箱 MD5 生成 Gravatar URL |
| 昵称     | `author_name`             | 直接展示                       |
| 网址链接 | `author_url`（如有）      | 昵称可点击跳转（新标签页打开） |
| 时间     | `create_at`               | 格式化展示                     |
| 内容     | `content`                 | 保持现有样式                   |
| 回复按钮 | 现有功能                  | 保持不变                       |
| 点赞按钮 | 现有功能                  | 保持不变                       |

#### 头像生成规则

- 使用 Gravatar 官方 URL：`https://www.gravatar.com/avatar/{md5(email)}?s=40&d=identicon`
- 尺寸建议：40x40（列表）或 48x48（详情）
- 默认头像使用 `identicon`（几何图形）
- 考虑国内网络环境，可配置镜像源（如 gravatar.loli.net、cravatar.cn）

#### 昵称链接逻辑

- 如果 `author_url` 存在且非空，昵称包裹在 `<a>` 标签中
- 链接属性：`target="_blank" rel="noopener noreferrer ugc"`
- 如果 `author_url` 为空，昵称仅显示为纯文本

### 4.3 类型定义更新

**位置**：`types/` 目录下的评论类型定义（如 `comment.ts`）

需要为 `Comment` 接口添加可选字段：

```typescript
author_url?: string  // 访客网站地址
```

---

## 五、管理后台变更（myblog-admin）

### 5.1 评论管理页面

**位置**：`views/CommentManage.vue`

#### 展示变更

评论列表中增加“网站”列，展示 `author_url`：

- 如果有值：显示可点击链接（截断显示）
- 如果为空：显示“—”
- 建议放在“邮箱”列之后

#### 其他

- 评论详情/审核弹窗可考虑显示 `author_url`
- 评论搜索/筛选功能无需修改

### 5.2 类型定义更新

**位置**：`types/` 目录下的评论类型定义

同步添加 `author_url` 字段

---

## 六、前后端联调要点

### API 对接确认清单

| 检查项       | 说明                                                      |
| ------------ | --------------------------------------------------------- |
| 评论提交接口 | 确认 `author_url` 字段被后端正确接收和存储                |
| 评论列表接口 | 确认返回数据中包含 `author_url`                           |
| 表单校验     | 前端和后端对 `author_url` 的校验规则一致（可选、URL格式） |
| 错误处理     | 后端返回的校验错误能正确展示在前端表单上                  |

### 测试场景

1. 提交包含网址的评论 → 列表中昵称可点击
2. 提交不包含网址的评论 → 昵称不可点击
3. 提交无效 URL（如 `test`）→ 后端应返回校验错误
4. 评论状态为 `pending` 时不显示在前台（保持现有行为）

---

## 七、文件修改清单（供 Copilot 定位）

### 后端（myblog-express）

| 文件路径                           | 修改类型 | 说明                              |
| ---------------------------------- | -------- | --------------------------------- |
| `controllers/commentController.js` | 修改     | 添加 `author_url` 字段处理和校验  |
| `middleware/validator.js`          | 修改     | 增加 `author_url` 的 URL 校验规则 |
| `models/Comment.js`                | 修改     | 确认模型包含 `author_url` 字段    |

### 博客前台（myblog-blog）

| 文件路径                     | 修改类型 | 说明                           |
| ---------------------------- | -------- | ------------------------------ |
| `types/comment.ts`           | 修改     | 添加 `author_url` 类型定义     |
| `components/CommentForm.vue` | 修改     | 增加网址输入框                 |
| `components/CommentList.vue` | 修改     | 增加头像和昵称链接逻辑         |
| `utils/gravatar.ts`          | 新增     | Gravatar URL 生成工具函数      |
| `api/comment.ts`             | 修改     | 确认 API 调用包含 `author_url` |

### 管理后台（myblog-admin）

| 文件路径                  | 修改类型 | 说明                       |
| ------------------------- | -------- | -------------------------- |
| `types/comment.ts`        | 修改     | 添加 `author_url` 类型定义 |
| `views/CommentManage.vue` | 修改     | 评论列表增加网站列         |
| `api/comment.ts`          | 修改     | 确认类型同步               |

---

## 八、实施优先级

### 第一阶段（核心功能）

- [ ] 数据库增加 `author_url` 字段
- [ ] 后端评论接口支持 `author_url`
- [ ] 博客前台评论表单增加网址输入框
- [ ] 博客前台评论列表显示 Gravatar 头像
- [ ] 博客前台评论列表支持昵称链接跳转

### 第二阶段（体验优化）

- [ ] localStorage 记住访客上次填写的昵称/邮箱/网址
- [ ] 前端 URL 格式实时校验（友好提示）
- [ ] 考虑 Gravatar 国内镜像配置

### 第三阶段（暂不实施）

- [ ] 访客自删评论功能（delete_token）
- [ ] 独立的留言板模块

---

## 九、注意事项

1. **XSS 防护**：`author_url` 和 `author_name` 在输出时需要转义，避免注入
2. **URL 协议**：如果用户输入的网址不含 `http://` 或 `https://`，建议前端/后端自动补充 `https://`
3. **nofollow 属性**：所有访客提供的网址链接应添加 `rel="nofollow ugc"`，符合搜索引擎规范
4. **评论状态**：继续保持 `pending` 待审核机制，防止垃圾评论
5. **邮箱隐私**：Gravatar 使用邮箱 MD5 值，不要在前端直接暴露原始邮箱
6. **向后兼容**：旧评论的 `author_url` 为 NULL，前端需正确处理

---

## 十、参考信息

### Gravatar URL 格式

```
https://www.gravatar.com/avatar/{MD5(email)}?s={size}&d=identicon
```

### URL 校验正则（参考）

```
^https?:\/\/(?:www\.)?[-a-zA-Z0-9@:%._\+~#=]{1,256}\.[a-zA-Z0-9()]{1,6}\b(?:[-a-zA-Z0-9()@:%_\+.~#?&\/=]*)$
```

（后端和前端可使用同一规则）

### localStorage 存储键名建议

```
`blog_comment_author`
```

存储结构：`{ name, email, url }`

---
