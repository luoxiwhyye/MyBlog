# 项目后续优化更新计划

> 本文档基于当前项目（2026-06-04）的代码质量和架构现状，按优先级列出后续优化方向与具体行动计划。

---

## 📊 当前状态总览

| 项目             | 当前版本      | 构建状态                | 类型检查 | 测试覆盖 |
| ---------------- | ------------- | ----------------------- | -------- | -------- |
| `myblog-express` | Express 5.2.1 | ✅                      | N/A (JS) | ❌ 无    |
| `myblog-blog`    | Nuxt 3.17.7   | ✅ 通过 (有 chunk 告警) | ✅ 通过  | ❌ 无    |
| `myblog-admin`   | Vue 3.5.30    | ✅                      | ✅ 通过  | ❌ 无    |

### 已知非阻塞告警

1. **博客前台 chunk 体积偏大**：部分客户端 chunk 超过 500 kB
2. **Node Deprecation Warning**：上游依赖触发 `DEP0155`（不影响功能）
3. **管理后台 API 地址写死**：`baseURL` 硬编码在 `src/utils/request.ts`

---

## 🎯 优先级分类说明

| 标识      | 含义                             | 建议周期 |
| --------- | -------------------------------- | -------- |
| 🔴 **P0** | 阻塞/安全风险，需立即处理        | 1-2 周   |
| 🟡 **P1** | 高优先级，影响开发体验或可维护性 | 1 个月   |
| 🟢 **P2** | 中优先级，提升质量和用户体验     | 3 个月   |
| ⚪ **P3** | 低优先级，锦上添花               | 6 个月+  |

---

## 🔴 P0 — 安全与稳定性

### 1.1 JWT Secret 生产安全检查

**现状**：`JWT_SECRET` 默认值为 `your-secret`，生产环境必须显式覆盖。

**行动计划**：

- [ ] 添加启动时 `JWT_SECRET` 为空或默认值的检测与告警
- [ ] 生产部署文档中强调必须配置强随机 `JWT_SECRET`
- [ ] 考虑添加 `.env.example` 模板文件

**影响范围**：`myblog-express/config/jwt.js`、`server.js`

---

### 1.2 管理后台 API 地址环境变量化

**现状**：`myblog-admin/src/utils/request.ts` 中 `baseURL` 写死为 `http://localhost:3000/api/v1`。

**行动计划**：

- [ ] 创建 `.env` / `.env.production` 文件
- [ ] 使用 Vite 的 `import.meta.env.VITE_API_BASE` 替代硬编码值
- [ ] 添加 `.env.example` 模板

**改动文件**：

```
myblog-admin/src/utils/request.ts
myblog-admin/.env.example (新增)
myblog-admin/env.d.ts (补充类型)
```

---

### 1.3 博主初始化失败可观测性增强

**现状**：`server.js` 中初始化失败仅输出日志但服务继续启动，容易掩盖问题。

**行动计划**：

- [ ] 增加更明确的初始化失败告警日志（包含错误详情）
- [ ] 考虑增加 `/health` 健康检查端点，包含初始化状态
- [ ] 区分"首次初始化"和"已存在博主跳过"的日志

**改动文件**：`myblog-express/server.js`、`myblog-express/utils/initBlogger.js`

---

## 🟡 P1 — 架构与可维护性

### 2.1 博客前台 Chunk 体积优化

**现状**：部分客户端 chunk 超过 500 kB，主要来自工具箱模块。

**行动计划**：

- [ ] 对 `utils/tools/processor.ts` 中的各工具算法做动态导入（按需加载）
- [ ] 将 Element Plus 改为按需引入（当前可能是全量引入）
- [ ] 分析 `highlight.js` 的语言包，仅保留常用语言
- [ ] 为大型工具组件手动分包（`vite.rollupOptions.output.manualChunks`）
- [ ] 运行 `nuxi analyze` 分析包体积构成

**改动文件**：

```
myblog-blog/nuxt.config.ts
myblog-blog/plugins/element-plus.ts
myblog-blog/utils/tools/processor.ts
```

**预期效果**：客户端首屏 JS 体积减少 40-60%

---

### 2.2 上传模块迁移至对象存储

**现状**：上传文件存储在本地 `uploads/` 目录，不适合多实例部署和水平扩展。

**推荐方案**：接入阿里云 OSS / 腾讯云 COS / AWS S3

**行动计划**：

- [ ] 抽象上传接口，定义 `StorageProvider` 抽象层
- [ ] 实现 `LocalStorageProvider`（当前逻辑迁移）
- [ ] 实现 `OSSStorageProvider`（对象存储）
- [ ] 通过环境变量切换存储后端
- [ ] 补充上传文件类型白名单和大小限制配置

**改动文件**：

```
myblog-express/config/upload.js
myblog-express/controllers/uploadController.js
myblog-express/utils/upload.js (新增抽象层)
```

---

### 2.3 请求与响应类型统一

**现状**：博客前台和管理后台各自定义 API 响应类型，存在重复和不一致。

**行动计划**：

- [ ] 在博客前台 `types/` 下统一 API 响应泛型
- [ ] 与管理后台的响应类型保持一致
- [ ] 为后端响应结构创建共享类型定义（可选：提取为独立包）

**改动文件**：

```
myblog-blog/types/index.ts
myblog-admin/src/types/api.ts
```

---

### 2.4 express-validator 升级至 v7+ 新 API

**现状**：项目使用 `express-validator ^7.3.1`，需确认是否使用最新的 `validationResult` 和链式 API 模式。

**行动计划**：

- [ ] 审计 `middleware/validator.js` 中的校验器写法
- [ ] 确保与 v7 API 兼容（v7 有 breaking changes）
- [ ] 统一错误消息格式

---

## 🟢 P2 — 测试与质量保障

### 3.1 后端 API 集成测试

**行动计划**：

- [ ] 引入 `vitest` 或 `jest` + `supertest`
- [ ] 为核心 CRUD 接口编写 smoke test（至少覆盖文章和认证）
- [ ] 添加测试数据库配置（独立于开发数据库）
- [ ] 在 `package.json` 中添加 `test` 脚本

**目标覆盖**：

```
✅ POST /api/v1/blogger/login        (认证)
✅ GET  /api/v1/articles             (公开读取)
✅ POST /api/v1/articles             (需认证的写操作)
✅ PUT  /api/v1/settings             (配置更新)
✅ GET  /api/v1/settings             (公开配置读取)
```

---

### 3.2 工具箱纯函数单元测试

**现状**：`utils/tools/` 下的工具算法均为纯函数，非常适合单元测试。

**行动计划**：

- [ ] 使用 `vitest`（Nuxt 生态原生支持）
- [ ] 为每个工具算法编写测试用例（正常输入 + 边界条件 + 异常输入）
- [ ] 优先覆盖编解码和加密类工具（最常用）

**测试用例示例**：

```
✅ Base64 编解码：正常字符串、空字符串、中文、emoji
✅ MD5 生成：已知输入 → 预期输出
✅ JSON 格式化：有效 JSON、无效 JSON、空对象
✅ 时间戳转换：10位/13位、负数时间戳、边界值
```

---

### 3.3 管理后台关键页面 E2E 测试（可选）

**行动计划**：

- [ ] 引入 Playwright 或 Cypress
- [ ] 覆盖登录 → 创建文章 → 发布 → 前台验证 这一核心链路
- [ ] 可作为 CI 流水线的一部分

---

## ⚪ P3 — 体验与运维增强

### 4.1 暗色主题支持

**现状**：Element Plus 支持暗色主题，但当前项目未启用。

**行动计划**：

- [ ] 为博客前台添加暗色/亮色切换
- [ ] 管理后台同步适配
- [ ] 使用 CSS 变量统一管理颜色
- [ ] 跟随系统主题偏好（`prefers-color-scheme`）

---

### 4.2 CI/CD 流水线搭建

**行动计划**：

- [ ] 使用 GitHub Actions 或 GitLab CI
- [ ] 阶段划分：
  - **Lint**：ESLint + Prettier 检查
  - **Type Check**：`npm run type-check`
  - **Test**：单元测试 + 集成测试
  - **Build**：生产构建验证
  - **Deploy**：自动部署到测试/生产环境

**示例流水线触发规则**：

- `main` 分支 PR → 运行 Lint + Type Check + Test + Build
- `main` 分支合并 → 自动部署到生产环境

---

### 4.3 错误监控与日志收集

**行动计划**：

- [ ] 后端接入日志收集（如 Winston 结构化日志）
- [ ] 前端接入错误监控（如 Sentry）
- [ ] 为 Worker 初始化失败、剪贴板 API 不可用等场景添加降级提示

---

### 4.4 工具箱体验增强

**行动计划**：

- [ ] 工具箱首页搜索支持模糊匹配和拼音搜索
- [ ] 命令面板 `Ctrl/Cmd + K` 支持键盘上下选择
- [ ] 工具使用统计埋点（可选，需隐私合规）
- [ ] 导出文件名优化（包含工具名和时间戳）

---

### 4.5 国际化（i18n）准备

**现状**：当前全站为中文。若后续有国际化需求，建议预留扩展点。

**行动计划**：

- [ ] 梳理需要国际化的文本（导航、提示信息、SEO 文本）
- [ ] 评估 `@nuxtjs/i18n` 接入成本
- [ ] 先实现中/英双语切换原型

---

### 4.6 数据库查询性能优化

**现状**：模型层为手写 SQL，部分列表查询可能需要索引优化。

**行动计划**：

- [ ] 对 `article` 表的 `status`、`type_id`、`created_at` 字段添加复合索引
- [ ] 对 `comment` 表的 `article_id`、`status` 字段添加索引
- [ ] 使用 `EXPLAIN` 分析典型查询的执行计划
- [ ] 考虑为高频读取的 `settings` 添加应用层缓存（内存或 Redis）

---

## 📋 汇总看板

| 编号 | 任务                      | 优先级 | 预计工时 | 状态      |
| ---- | ------------------------- | ------ | -------- | --------- |
| 1.1  | JWT Secret 安全检查       | 🔴 P0  | 0.5d     | ⬜ 待开始 |
| 1.2  | API 地址环境变量化        | 🔴 P0  | 0.5d     | ⬜ 待开始 |
| 1.3  | 初始化失败可观测性        | 🔴 P0  | 0.5d     | ⬜ 待开始 |
| 2.1  | Chunk 体积优化            | 🟡 P1  | 2d       | ⬜ 待开始 |
| 2.2  | 上传迁移对象存储          | 🟡 P1  | 3d       | ⬜ 待开始 |
| 2.3  | 类型定义统一              | 🟡 P1  | 1d       | ⬜ 待开始 |
| 2.4  | express-validator v7 审计 | 🟡 P1  | 0.5d     | ⬜ 待开始 |
| 3.1  | 后端 API 集成测试         | 🟢 P2  | 2d       | ⬜ 待开始 |
| 3.2  | 工具箱单元测试            | 🟢 P2  | 2d       | ⬜ 待开始 |
| 3.3  | E2E 测试（可选）          | 🟢 P2  | 3d       | ⬜ 待开始 |
| 4.1  | 暗色主题                  | ⚪ P3  | 2d       | ⬜ 待开始 |
| 4.2  | CI/CD                     | ⚪ P3  | 2d       | ⬜ 待开始 |
| 4.3  | 错误监控                  | ⚪ P3  | 1d       | ⬜ 待开始 |
| 4.4  | 工具箱体验增强            | ⚪ P3  | 1.5d     | ⬜ 待开始 |
| 4.5  | i18n 预备                 | ⚪ P3  | 1d       | ⬜ 待开始 |
| 4.6  | 数据库索引优化            | ⚪ P3  | 1d       | ⬜ 待开始 |

---

## 🗓 建议迭代节奏

### 第 1-2 周：安全加固（P0）

> 目标：消除安全风险，补齐基础配置

1. JWT Secret 检测 + 环境变量模板
2. 管理后台 API 地址环境变量化
3. 初始化流程日志和健康检查端点

### 第 3-6 周：架构优化（P1）

> 目标：提升可维护性，降低技术债务

1. 博客前台 chunk 拆包优化
2. 上传模块抽象层 + 对象存储接入
3. 前后端类型统一

### 第 7-12 周：质量保障（P2）

> 目标：建立自动化测试体系

1. 后端核心 API 集成测试
2. 工具箱纯函数单元测试
3. CI 流水线初步搭建

### 第 13 周+：体验增强（P3）

> 目标：持续打磨用户体验

1. 暗色主题
2. 工具箱交互增强
3. 错误监控接入
4. 性能优化与国际化

---

## 📝 版本记录

| 日期       | 版本 | 说明                                     |
| ---------- | ---- | ---------------------------------------- |
| 2026-06-04 | v1.0 | 初始版本，基于项目交接文档和代码现状输出 |
