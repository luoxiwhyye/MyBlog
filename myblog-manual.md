# MyBlog 项目说明文档

## 1. 文档目的与适用对象

本说明文档用于帮助开发、测试、运维和交付人员快速理解并接手 MyBlog 项目，覆盖以下内容：

- 系统结构与模块职责
- 本地开发、联调、构建、部署流程
- 数据模型与字段约定
- 功能边界与关键业务流程
- 常见故障排查和运维建议

适用对象：

- 新加入项目的前端/后端开发
- 测试工程师
- 运维部署人员
- 需要进行二次开发的维护人员

---

## 2. 项目总体架构

### 2.1 架构形态

MyBlog 采用前后端分离架构，包含三部分：

- 读者端前台（myblog-blog）：面向访客内容浏览与互动。
- 管理端后台（myblog-admin）：面向管理员内容运营与配置管理。
- 后端服务（myblog-express）：统一 API 层、鉴权层、数据访问层、上传能力。

### 2.2 逻辑分层

- 路由层（routes）：定义 API 路径与中间件链。
- 控制层（controllers）：参数解析、权限校验、流程编排。
- 模型层（models）：数据库读写与聚合查询。
- 中间件层（middleware）：认证、角色、校验、错误处理。
- 工具层（utils/config）：通用响应、分页、上传、数据库连接等。

---

## 3. 目录结构

根目录核心结构：

- `myblog-express/`：后端服务
- `myblog-vue/myblog-blog/`：前台应用
- `myblog-vue/myblog-admin/`：后台应用
- `myblog-express/myblog.sql`：数据库表结构参考

后端子结构（摘要）：

- `controllers/`：业务入口
- `models/`：数据库访问
- `routes/`：API 路由
- `middleware/`：认证与校验
- `config/`：数据库、JWT、上传配置
- `uploads/`：上传文件目录

前端子结构（摘要）：

- `src/views/`：页面级组件
- `src/components/`：通用组件
- `src/stores/`：Pinia 状态管理
- `src/api/`：接口封装
- `src/router/`：路由配置
- `src/utils/`：请求和工具函数

---

## 4. 技术栈与版本建议

### 4.1 后端

- Node.js 18+
- Express
- MySQL 8+
- mysql2
- JWT（jsonwebtoken）
- Multer（上传）
- express-validator（参数校验）

### 4.2 前端

- Vue 3 + TypeScript
- Vite
- Pinia
- Vue Router
- Axios
- Element Plus
- ECharts（后台可视化）

---

## 5. 业务模块说明

### 5.1 认证与权限

- 管理端登录后获取 JWT。
- 受保护接口需附带 `Authorization: Bearer <token>`。
- 通过角色中间件限制管理员接口（例如文章管理、配置修改）。

### 5.2 文章模块

- 支持创建、编辑、发布、草稿。
- 支持封面上传。
- 支持软删除（进入回收站）。
- 回收站支持恢复与彻底删除。
- 前台文章详情访问会增加浏览量。

### 5.3 分类与标签模块

- 分类/标签的增删改查。
- 统计关联文章数量。
- 写文章页支持全量选择分类与标签。

### 5.4 评论模块

- 访客评论与回复。
- 管理端审核状态流转：`pending`/`approved`/`spam`/`deleted`。
- 前台评论支持排序与分页。
- 评论回收站支持恢复与彻底删除。
- 管理端评论主列表默认排除 `deleted`，回收站通过 `status=deleted` 独立查询。

### 5.5 网站配置模块

- 支持文本和图片配置。
- `site_logo`、`site_favicon` 在前后台统一生效。
- 配置图片可按 `setting_key` 独立目录保存。

### 5.6 仪表盘模块

- 统计指标：总文章、总评论、总浏览、待审核评论。
- 图表：发布趋势、分类分布。

---

## 6. 关键业务流程

### 6.1 文章删除流程

1. 管理端文章列表点击删除。
2. 后端执行软删除，设置 `deleted_at`。
3. 文章进入回收站。
4. 管理员可恢复（清空 `deleted_at`）或彻底删除（物理删除）。

### 6.2 配置图片更新流程

1. 管理端上传图片并获取 URL。
2. 保存配置时提交图片 URL 对应 key。
3. 后端更新设置项，必要时清理旧图片。
4. 前台与后台在启动或刷新配置后同步生效（logo/favicon）。

### 6.3 评论审核流程

1. 前台评论默认进入待审核状态。
2. 管理端审核通过后变为 `approved`。
3. 前台仅展示已审核评论。

### 6.4 评论回收站流程

1. 管理端在评论列表执行“移入回收站”，后端将评论及其回复状态置为 `deleted`。
2. 评论从主列表移除，仅在回收站视图展示。
3. 管理员可执行“恢复为待审核”（状态改为 `pending`）或“彻底删除”（物理删除）。

---

## 7. 本地开发与联调

### 7.1 后端

1. 进入 `myblog-express`
2. 执行 `npm install`
3. 创建数据库并导入 `myblog.sql`
4. 配置环境变量（数据库连接、JWT、端口）
5. 执行 `npm run dev`

### 7.2 前台

1. 进入 `myblog-vue/myblog-blog`
2. 执行 `npm install`
3. 执行 `npm run dev`

### 7.3 后台

1. 进入 `myblog-vue/myblog-admin`
2. 执行 `npm install`
3. 执行 `npm run dev`

联调建议：

- 先启动后端，再启动前台和后台。
- 确认前端 API baseURL 与后端端口一致。
- 管理端登录后再验证管理接口。

---

## 8. 构建与发布

### 8.1 构建

- 前台：`myblog-vue/myblog-blog` 执行 `npm run build`
- 后台：`myblog-vue/myblog-admin` 执行 `npm run build`

### 8.2 发布策略建议

- 后端使用 PM2 或容器方式部署。
- 前端静态资源可部署至 Nginx。
- `uploads/` 目录应挂载持久卷，避免重启丢失。
- 生产环境启用 HTTPS 与反向代理缓存策略。

---

## 9. 数据模型与字段约定

### 9.1 命名规则

- 数据库字段：`snake_case`
- 接口与前端：`camelCase`

### 9.2 关键字段

- 评论时间：库字段 `create_at`，接口字段 `createdAt`
- 文章软删除：`deleted_at`
- 评论状态：`pending` / `approved` / `spam` / `deleted`

### 9.3 上传文件路径约定

- 文章封面：`uploads/article/cover/...`
- 正文图片：`uploads/article/content/...`
- 站点配置图片：`uploads/setting/image/{setting_key}/...`

---

## 10. API 使用要点（摘要）

- 文章列表：支持分页、关键词、分类、标签、状态筛选。
- 回收站列表：仅管理员可访问。
- 文章恢复/彻底删除：仅管理员可操作。
- 配置更新：支持文本与图片 URL 更新。
- 上传接口：需管理员鉴权并传上传场景参数。
- 评论列表：前台固定仅 `approved`；管理端默认排除 `deleted`，可按状态筛选。
- 评论回收站：使用 `status=deleted` 查询，支持 `/:id/restore` 与 `/:id/hard`。

---

## 11. 测试建议

### 11.1 功能测试

- 文章生命周期：创建 -> 编辑 -> 软删除 -> 恢复 -> 彻底删除
- 评论流程：提交 -> 审核 -> 前台展示 -> 移入回收站 -> 恢复/彻底删除
- 配置同步：修改 logo/favicon 后前后台展示一致

### 11.2 边界测试

- 空值与非法参数输入
- 大文件上传与非图片上传
- 多端同时操作同一资源

### 11.3 回归关注点

- 回收站数据与普通列表互斥
- 配置保存后旧图片清理是否误删
- 登录过期后的路由跳转

---

## 12. 常见问题排查

### 12.1 页面空白或接口 401

- 检查 token 是否失效。
- 检查请求头是否带 Bearer token。

### 12.2 上传成功但图片不显示

- 检查返回 URL 是否可访问。
- 检查后端静态目录映射是否覆盖 `uploads`。

### 12.3 配置修改后前台未更新

- 检查设置项 key 是否正确。
- 刷新页面并确认 settings 拉取成功。
- 检查 favicon 是否被浏览器缓存。

---

## 13. 运维与安全建议

- 为关键管理接口增加操作审计日志。
- 对彻底删除保留二次确认和权限校验。
- 限制上传类型和大小并定期扫描 uploads 目录。
- 使用强 JWT 密钥并定期轮换。
- 建立数据库定时备份与恢复演练机制。

---

## 14. 后续规划建议

- 文章版本历史与差异对比
- 全文检索与搜索排序增强
- 评论风控（频率限制、敏感词）
- 多角色权限体系（编辑、审核员）
- CI/CD 自动化测试与发布

---

## 15. 维护记录

- 建议在每次重要功能上线后，补充版本记录（功能、影响范围、回滚方案）。
- 建议将数据库变更与接口变更同步写入变更日志，便于追溯。
