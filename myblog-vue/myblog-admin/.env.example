# -----------------------------------------------------------
# MyBlog Admin 环境变量模板
# 复制此文件为 .env 并修改对应值
# -----------------------------------------------------------

# 后端 API 基础地址
# 开发环境使用相对路径 /api/v1（由 vite.config.ts 代理到本机 3000），
# 同 WiFi 手机通过 http://<电脑IP>:5173 访问时同样有效。
# 生产构建时由 Dockerfile ARG VITE_API_BASE 注入真实地址。
VITE_API_BASE=/api/v1
